apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "department-web-front-end.fullname" . }}-test-connection"
  labels:
    {{- include "department-web-front-end.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "department-web-front-end.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
